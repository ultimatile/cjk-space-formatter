[project]
name = "cjk-latin-space"
version = "0.1.0"
description = "Shared core: collapse CJK<->Latin/digit spaces within a plain run (not published)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cjk_latin_space"]
